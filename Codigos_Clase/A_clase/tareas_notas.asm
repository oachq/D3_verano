; Miercoles 8 de junio. 
; Implementado los ejercicios 2 y 3
; nibbles y multiplicacion por sumas sucecivas.

;*****************************************************************************************************

; Jueves 9 de junio. 
;Programa 1:
;        programa que si el bit 0 del port A es igual a 1, enciende los bits pares del puerto B y si no 
;        enciendo los impares durante 1 seg. 

; programa 2:  
;       programa que compara el dato del port A con el ultimo numero de tu ID y habra 3 posibilidades:
;       a) Si son iguales, se encienden todos los leds del port B 
;       b) Si el port A es mayor se enciende el port D 
;       c) Si el puerto A es menor se enciende los nibbles altos de los ports B y D 
    ;NOTA: no usar comparaciones        

;*****************************************************************************************************

; viernes 17 de junio.
; 3) Diseñe un programa que recibe dos variables de 8 bits cada una, la primera contiene el tiempo de
;    ensendido y la segunda el tiempo de apagado, utilice esos tiempos para encender y apagar un bit 
;    del puerto B con el TMR0 
;material: solo codigo no implementacion. 


; Lunes 20 de junio
; UNIR LOS CODIGOS DEL TECLADO EN EL LCD y telcado 4x4 llenando la linea 1 que continue con la linea 2
; y cuando termine de escribir en la linea 2 que borre el contenido y vuelva a iniciar el ciclo.
; material: teclado, 4 R 10 k, lcd 

; 2) Diseñe un programa que rote un led del puerto B cada segundo utilizando el TMR0.
; durante 10seg. utilizando el TMR1.  
; material: barra de leds 
;*****************************************************************************************************


;*****************************************************************************************************
; Lunes 27
; Realice un programa que genera una señal cuadrada en un pin de un puerto cuyo periodo este determinado 
; por el valor leido en el ADC, utilizando el TMR0
; Nota: tomar valores enteros en la entrada de voltajes.
; Material: solo simulado 

;; consejo terminar para el viernes para estar al cien pal examen del viernes
;*****************************************************************************************************
